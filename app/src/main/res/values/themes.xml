<resources>
    <!-- Base application theme. -->
    <style name="lightTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/lightBackground</item>
        <item name="android:textColor">#2f4f4f</item>
        <item name="android:editTextColor">#2f4f4f</item>
    </style>

    <style name="spinner_style" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="button_style" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/darkBackground</item>
    </style>

</resources>